// import express
const express = require('express');
const router =  express.Router();


// not sure if we need the start router too, maybe just game is enough

router.get('/start', (req,res) => {

})

